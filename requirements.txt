# Lower-bound constraints (compatible with Python 3.10–3.13).
# If you want fully reproducible builds, re-pin with `pip freeze` after install.

# Core
streamlit>=1.40
pydantic>=2.9
python-dotenv>=1.0

# LangChain core + community
langchain>=0.3.13
langchain-core>=0.3.28
langchain-community>=0.3.13
langchain-text-splitters>=0.3.4

# LLM providers (pluggable)
langchain-anthropic>=0.3
langchain-openai>=0.2.14
langchain-google-genai>=2.0
langchain-groq>=0.2
langchain-huggingface>=0.1.2
langchain-ollama>=0.2

# Embeddings + vector store
sentence-transformers>=3.3
faiss-cpu>=1.9

# Document loaders
pypdf>=5.1

# PDF export of chat / quiz outputs
# Audited 2026-05-14: no known CVEs (GHSA-rwmj-c32v-585v targets PHP FPDF, not fpdf2).
fpdf2>=2.8.7,<3.0.0

# HuggingFace runtime
huggingface-hub>=0.27
transformers>=4.47
# torchvision is not used by our code, but recent `transformers` versions
# eagerly import image processors (zoedepth) that need it.
torchvision>=0.20

# Inline chart rendering (Plotly figures embedded in chat replies).
# Audited 2026-05-27: plotly Python has no known CVEs (Snyk DB); the only
# historical advisory (GHSA-2fqv-h3r5-m4vf) was a 2017 XSS in plotly.js
# fixed in 1.16.0. Math expressions inside chart blocks are evaluated by
# a custom ast-whitelist validator (see app/charts.py), so no symbolic
# math library is needed and no attack surface from sympy/sympify is
# inherited.

plotly>=5.20,<7.0

# Static image export for embedding charts into PDFs.
# Audited 2026-05-27: 0 GitHub Advisory entries for the Python "kaleido"
# package. v1.x relies on a system-installed Chrome rather than bundling
# its own Chromium binary, so Chrome patches apply normally. On hosts
# without Chrome (e.g. a default HuggingFace Space image), PDF chart
# embedding gracefully falls back to a "[Chart: <title>]" placeholder.
kaleido>=1.2.0,<2.0
